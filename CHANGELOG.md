# 변경 이력 (Changelog)

## 1.0.9 (build 3) — 2026-06-05

### 버그 수정
- **근무 날짜 변경 시 알람 중복/이전 알람 발생 문제 수정**
  - 원인: 알림 재예약이 홈 화면 첫 진입(`initState`) 시 한 번만 실행됐고, 앱이 `IndexedStack` 구조라 홈 화면이 계속 살아 있어 재실행되지 않았습니다. 그 결과 캘린더에서 근무를 바꿔도 OS에 남아 있던 이전 일정 기준 알림이 그대로 발사됐습니다.
  - 조치: 일정 의존 알림 재예약 로직을 `NotificationScheduler.rescheduleForSchedule()` 한 곳으로 중앙화하고, 재예약 전 항상 이전 슬롯(근무·스마트수면·카페인·사전알림)을 모두 취소하도록 변경했습니다.
  - 호출 지점: 근무 추가/삭제/패턴 적용, 홈 로드, 앱 포그라운드 복귀, 알림 설정 토글, 근무 시간 변경.
  - 추가로, 스마트 수면 알림이 재예약 시 기존 알림을 취소하지 않아 누적되던 문제도 함께 해결했습니다.

- **수면 자동 동기화가 동작하지 않던 문제 수정 (iOS 0건)**
  - 원인: `health` 패키지(v13) 사용 전 필수인 `Health().configure()` 호출이 누락되어 있었습니다. 이 경우 iOS HealthKit이 권한 요청·데이터 조회를 조용히 빈 결과로 반환합니다.
  - 조치: `HealthDataService`에 `_ensureConfigured()`를 추가하고, 권한 요청/확인 진입점에서 최초 1회 `configure()`가 보장되도록 했습니다. 동기화된 수면 기록이 주간 리포트 분석에도 반영됩니다.

### 기타
- 앱 포그라운드 복귀 시 건강 데이터 자동 동기화를 재시도하도록 개선.
- 알림 스케줄링 중복 코드(홈/설정/근무시간 화면)를 단일 스케줄러로 통합.

### 알려진 후속 과제
- iOS에서 `requestAuthorization`은 사용자가 거부해도 성공(`true`)을 반환할 수 있어, 권한 캐시가 거짓 양성일 가능성이 있습니다. 수면 데이터가 계속 비어 있으면 건강 데이터 동기화 토글을 껐다 켜서 권한을 갱신해야 합니다. (향후 권한 재확인 UX 보강 필요)
