# 변경 이력 (Changelog)

## 1.0.10 (build 4) — 2026-09-05

### 버그 수정
- **근무를 바꿔도 이전 근무 알림이 뜨던 문제 (재발) 수정**
  - 원인: `shifts` 테이블의 기본키가 uuid뿐이라, 근무를 수정할 때마다 같은 날짜에 **새 행이 추가**됐습니다(`ConflictAlgorithm.replace`는 uuid에만 걸려 충돌하지 않음). 바꾼 직후엔 메모리 상태가 맞지만, 홈 진입·캘린더 월 이동으로 DB를 다시 읽으면 동점 정렬 순서에 따라 옛 근무가 이겨서 그대로 재예약됐습니다.
  - 조치: `idx_shifts_date`를 UNIQUE로 바꿔 **날짜당 1행**을 강제하고(dbVersion 5), 기존에 쌓인 중복 행을 날짜별 최신 1건만 남기고 정리하는 마이그레이션을 추가했습니다. `addShift`는 그 날짜의 기존 행 id를 재사용해 제자리 갱신합니다.
  - 조회도 확정적으로: `getShiftsForMonth`는 `date ASC, created_at ASC`, `getShiftForDate`는 `created_at DESC` 기준.

- **야간근무 낮잠·취침 알림이 하루 일찍 오던 문제 수정**
  - 원인: 스케줄러가 "오늘 날짜 + 내일 근무" 조합으로 알림을 잡았습니다. 주간(06:00)·오후(14:00)는 우연히/하드코딩 보정으로 맞았지만 **야간(22:00)만 보정이 없어** "오늘 밤 야간근무" 알림이 근무 전날 낮에 발송됐습니다.
  - 조치: 취침 시각을 **근무 시작 − 8시간**으로 일반화해 근무 자체에서 도출합니다. 06:00 근무는 전날 22:00, 22:00 근무는 당일 14:00으로 자연스럽게 떨어지고 "오늘/내일" 문구도 발송일 기준으로 계산됩니다. 정오 낮잠 알림도 근무 당일로 이동해, 이전엔 못 잡던 **오늘 밤 야간근무**의 사전 알림도 예약됩니다.

- **Android 12 이상에서 알림이 아예 예약되지 않던 문제 수정**
  - 원인: `SCHEDULE_EXACT_ALARM` 권한 없이 정확 알람(`exactAllowWhileIdle`)으로 예약하면 플러그인이 예외를 던지는데, 홈 로드의 포괄 `try/catch`가 이를 삼켜 조용히 실패했습니다.
  - 조치: 매니페스트에 권한을 선언하고, 거부된 경우 **비정확 알람으로 자동 폴백**하도록 했습니다(Android 14+는 기본 거부).

- **재부팅 후 예약 알림이 모두 사라지던 문제 수정**: `RECEIVE_BOOT_COMPLETED` 권한과 플러그인 부트 리시버를 매니페스트에 추가했습니다.

- **데이터 초기화·삭제 후 유령 근무가 남던 문제 수정**: `loadShiftsForMonth`가 해당 월 캐시를 비우지 않고 병합만 해서, DB에서 지워진 근무가 메모리에 남아 다시 알림으로 예약됐습니다. 초기화 시 전체 캐시를 비우는 경로도 추가했습니다.

- **월말에 다음 근무/휴무가 표시되지 않던 문제 수정**: 홈이 이번 달만 로드해 다음 달 근무를 못 봤습니다. 다음 달까지 로드하고, 알림 스케줄러는 메모리에 없는 날짜를 DB에서 채웁니다.

- **일요일 저녁에 앱을 열면 주간 리포트 알림이 예약되지 않던 문제 수정**: 이번 주 20시가 지나면 그냥 종료하던 로직을 다음 주로 넘기도록 했습니다.

- **지나간 근무가 알림 슬롯을 낭비하던 문제 수정**: 이미 시간이 지난 근무도 7개 슬롯 중 하나를 차지해, 그만큼 뒤쪽 근무 알림이 예약되지 않았습니다.

- **워치/위젯이 자정 이후 어제 근무를 표시하던 문제 수정**
  - 원인: 폰이 App Group에 쓰는 `widget_today_shift_*` 키에 **날짜가 없어**, 폰 앱이 마지막으로 실행된 시점의 "오늘"이 그대로 고정됐습니다. 컴플리케이션은 자정에 타임라인을 갱신하도록 제대로 짜여 있었지만 갱신 후 읽는 값이 여전히 어제 것이라, 폰을 열기 전까지 워치 페이스가 틀린 근무를 표시했습니다. 워치 근무 종료 알림도 어제 시간으로 예약됐습니다.
  - 조치: `WidgetDataReader`가 날짜가 들어있는 `widget_week_shifts`(14일치)에서 오늘을 찾아 쓰도록 바꿨습니다. 폰을 열지 않아도 자정마다 스스로 맞춰집니다. 이를 위해 주간 JSON에 근무 시작/종료 시각을 추가했고, 구버전 데이터는 기존 키로 폴백합니다. `다음 휴무까지` 일수도 같은 방식으로 계산합니다.
  - 날짜 파싱에 고정 로캘/그레고리력을 지정해, 비그레고리력 지역 설정에서 오늘 매칭이 깨지지 않도록 했습니다.

- **워치 알림이 폰 설정을 무시하고 중복되던 문제 수정**
  - 워치가 이동 시간 설정과 무관하게 "10분 후 시작"을 자체 예약해, 폰의 "지금 출발하세요!"(기본 60분 전)와 서로 다른 알림이 두 번 왔습니다.
  - 조치: 출발 알림은 폰 단일 소스로 통일했습니다(아이폰 미사용 시 워치로 자동 미러링). 워치는 폰에 없는 **근무 종료 알림만** 담당하며, 야간근무처럼 자정을 넘기는 근무에서 엉뚱한 시각에 걸리지 않도록 연·월·일까지 지정해 예약합니다.

### 추가
- **알림 상태 화면** (설정 → 알림 설정 → 알림 상태 확인)
  - 알림 권한/정확 알람 허용 여부를 표시하고, 꺼져 있으면 경고 카드와 함께 시스템 설정으로 유도합니다.
  - **예정된 알림 목록**을 시각·종류·문구까지 그대로 보여줍니다. 스케줄러가 실제로 사용하는 계산기와 동일한 소스라, 화면에 보이는 것이 곧 예약되는 것입니다.
  - 테스트 알림 즉시 발송 버튼.

### Apple Watch
- **워치 앱이 iOS 앱에 포함되지 않던 문제 수정.** `ChangeWatch` 타깃과 뷰·컴플리케이션 코드는 모두 있었지만 Xcode 프로젝트 배선이 두 군데 끊겨 있었습니다: `Embed Watch Content` 복사 페이즈가 어떤 타깃에도 붙어있지 않았고, ChangeWatch용 `PBXTargetDependency` 객체가 Runner의 dependencies 목록에서 빠져 있었습니다. 그 결과 워치 앱은 단독으로는 빌드되지만 아카이브에는 들어가지 않아, **지금까지 한 번도 출시된 적이 없습니다.** 두 연결을 복구해 이번 버전부터 함께 배포됩니다.
- 워치 타깃에 `SUPPORTED_PLATFORMS`가 없어 iOS 대상 빌드에서 iOS로 특수화되며 컴파일이 깨지던 문제도 함께 수정했습니다.
- **워치 앱 아이콘 추가** (`tool/generate_watch_icon.dart`). watchOS는 원형 마스크를 직접 적용하므로 둥근 모서리 없는 정사각형으로, 심사 거부 사유인 알파 채널이 없도록 RGB로 생성합니다. 폰 아이콘과 동일한 앰버 그라데이션 + 시계 도안입니다.
- **워치 앱 스크린샷 생성 스크립트 추가** (`tool/capture_watch_screenshots.sh`). App Store 제출용 4종을 시뮬레이터에서 자동 캡처합니다.
- **`readLastUpdated()`가 항상 nil을 반환하던 문제 수정.** 폰이 쓰는 Dart `toIso8601String()`에는 타임존 지시자가 없는데 `ISO8601DateFormatter`는 이를 거부합니다. 그래서 "마지막 업데이트" 표시가 실제 기기에서도 한 번도 렌더링된 적이 없었습니다. 로컬 타임 파싱 폴백을 추가했습니다.
- **워치 타깃 Debug 컨피그에 `SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG` 누락 수정.** `#if DEBUG` 블록이 모든 구성에서 컴파일 제외되어 디버그 전용 코드가 조용히 죽어 있었습니다.
- **번들 버전 불일치 수정**: 워치·컴플리케이션·위젯 타깃이 `1.0 / 1`로 고정돼 있어 iOS 앱과 달랐습니다(ITMS-90473 거부 사유). 세 타깃을 앱 버전에 맞추고, 워치 plist의 하드코딩 값을 `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)` 변수로 바꿔 앞으로는 프로젝트 설정 한 곳만 고치면 되도록 했습니다.

### 내부 구조
- 알림 시각·문구 결정 로직을 순수 함수(`NotificationPlanner`)로 분리했습니다. `NotificationScheduler`는 일정 조회와 OS 전달만 담당하고, 알림 상태 화면과 테스트가 같은 계산기를 공유합니다.
- `NotificationService`에서 근무/취침/카페인/사전알림별 개별 메서드를 제거하고 단일 `schedulePlanned`로 통합했습니다. 미사용 `scheduleSleepReminder`도 삭제.
- 테스트 23개 추가: 근무 타입별 취침 시각, 슬롯 배정, 월 경계, 설정 토글, "과거 시각 예약 없음" 불변식, DB 중복행 마이그레이션(sqflite_common_ffi).

## 1.0.9 (build 3) — 2026-06-05

### 버그 수정
- **근무 날짜 변경 시 알람 중복/이전 알람 발생 문제 수정**
  - 원인: 알림 재예약이 홈 화면 첫 진입(`initState`) 시 한 번만 실행됐고, 앱이 `IndexedStack` 구조라 홈 화면이 계속 살아 있어 재실행되지 않았습니다. 그 결과 캘린더에서 근무를 바꿔도 OS에 남아 있던 이전 일정 기준 알림이 그대로 발사됐습니다.
  - 조치: 일정 의존 알림 재예약 로직을 `NotificationScheduler.rescheduleForSchedule()` 한 곳으로 중앙화하고, 재예약 전 항상 이전 슬롯(근무·스마트수면·카페인·사전알림)을 모두 취소하도록 변경했습니다.
  - 호출 지점: 근무 추가/삭제/패턴 적용, 홈 로드, 앱 포그라운드 복귀, 알림 설정 토글, 근무 시간 변경.
  - 추가로, 스마트 수면 알림이 재예약 시 기존 알림을 취소하지 않아 누적되던 문제도 함께 해결했습니다.

- **수면 자동 동기화가 동작하지 않던 문제 수정 (iOS 0건)**
  - 원인: `health` 패키지(v13) 사용 전 필수인 `Health().configure()` 호출이 누락되어 있었습니다. 이 경우 iOS HealthKit이 권한 요청·데이터 조회를 조용히 빈 결과로 반환합니다.
  - 조치: `HealthDataService`에 `_ensureConfigured()`를 추가하고, 권한 요청/확인 진입점에서 최초 1회 `configure()`가 보장되도록 했습니다. 동기화된 수면 기록이 주간 리포트 분석에도 반영됩니다.

### 기타
- 앱 포그라운드 복귀 시 건강 데이터 자동 동기화를 재시도하도록 개선.
- 알림 스케줄링 중복 코드(홈/설정/근무시간 화면)를 단일 스케줄러로 통합.

### 알려진 후속 과제
- iOS에서 `requestAuthorization`은 사용자가 거부해도 성공(`true`)을 반환할 수 있어, 권한 캐시가 거짓 양성일 가능성이 있습니다. 수면 데이터가 계속 비어 있으면 건강 데이터 동기화 토글을 껐다 켜서 권한을 갱신해야 합니다. (향후 권한 재확인 UX 보강 필요)
